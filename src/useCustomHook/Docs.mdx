import { Canvas, Meta, Description } from "@storybook/blocks";
import * as CustomHook from "./useCustomHook.stories";

<Meta of={CustomHook} />

# useCustomHook

이 커스텀 훅은 값을 증가시키는 기능을 제공합니다.

<Canvas of={CustomHook.Default} />
